# 目录整理说明

## 本次深度整理结果

- CLI 迁移到 `apps/cli`
- GUI 迁移到 `apps/gui`
- 根目录仅保留文档、脚本、忽略规则和工作区配置
- GUI 历史录制样本归档到 `apps/gui/recordings_legacy`

## 收益

- 结构清晰，职责边界明确
- CLI 与 GUI 可独立构建、独立发布
- 后续提取共享模块更容易

## 下一阶段建议

1. 在 `shared/` 抽离通用 checker/recorder 能力。
2. 给 `apps/gui/pkg/*` 添加最小单元测试。
3. 增加 CI（CLI/GUI 后端测试 + GUI 前端构建）。
