# 更新日志

本文档记录项目已经完成的功能维护和缺陷修复。新记录按日期倒序添加。

## 2026-06-29

### 强制 Cookie 状态检查

- 强制 Cookie 主播状态检查遇到 `streamserver.php` 超时时，会短暂等待后自动重试一次，减少同批主播因旧网页接口慢响应而直接进入异常。
- 监控页右侧主播详情卡新增单主播画质选择，可直接覆盖全局画质策略。
- 监控页右侧移除重复度较高的“完整详情”按钮，保留主页、历史录播、删除和监听操作。

### 画质和码率可视化

- 录制历史新增平均码率展示，旧历史记录会根据文件大小和录制时长自动估算。
- 新录制完成后会通过 FFprobe 写入媒体信息，包括总码率、分辨率、帧率、视频编码和音频编码。
- 全局画质策略扩展为原始/最高、高画质、中档稳定、低画质、自动尝试和仅保存音频。
- 单主播画质策略支持同样的覆盖选项，适合把文件特别大的主播单独调低。
- “仅保存音频”会减少最终文件体积，但不保证减少 HLS 下载流量。

## 2026-06-28

### 暂停监听稳定性

- 修复手动暂停主播后，旧 worker 延迟回传 `recording` 状态导致主播重新显示为“录制中”的问题。
- 暂停状态下会忽略旧 worker 的状态、日志和错误事件，但仍保留最终 `result` 事件用于写入“手动停止”历史记录。
- 增加暂停状态下忽略旧 worker 状态事件的单元测试。

### 历史录播入口

- 监控页右侧主播详情卡新增“历史录播”按钮。
- 点击后自动切换到录制历史页，并按当前主播 ID 筛选历史录播，无需再手动输入搜索词。

## 2026-06-18

### 受限直播压制

- 修复强制 Cookie 主播进入“受限直播”后，独立 worker 下一轮检查又把状态刷回“监听中”的问题。
- 受限直播的本场识别规则改为：只有两个明确且不同的 `movie_id` 才视为新直播；`created` 或 `current-live` 等不稳定标识继续按同一场处理。
- 受限状态下不再重复启动 FFmpeg，也减少每分钟重复输出 Cookie 解析日志。

### FFmpeg 卡流处理

- 增加 FFmpeg 媒体进度看门狗：开录后长时间无媒体进度，或媒体进度停止增长超过阈值时，自动停止当前录制进程。
- 进度停滞导致的录制会保留已生成文件，但历史状态归类为 `failed_network`，避免误显示为普通完成。
- 扩展网络异常分类，覆盖 `Connection failed`、`input/output error`、`end of file` 和媒体进度停滞等场景。

## 2026-06-08

### GUI 与后端状态一致性

- 新增后端主播运行状态快照，刷新列表后可恢复真实的录制中、异常和受限直播状态。
- 刷新状态时保留当前标题、录制时长和媒体时长。
- 修复监控页历史异常统计漏掉 `failed_auth`、`failed_network` 等细分异常的问题。
- “全部开始”改为使用后端错峰启动逻辑，并真正遵循设置中的错峰间隔。
- 支持将错峰间隔设置为 `0`，表示立即启动。
- 新的“全部开始/全部暂停”操作会取消仍未执行的旧批量启动队列。

### 设置与 worker

- FFmpeg/FFprobe 检测和健康检查现在使用页面当前填写的设置，无需先保存。
- 保存影响 worker 的设置后，空闲 worker 会自动重载。
- 正在录制的 worker 会等待本次录制结束，再应用新配置。
- OAuth 换取 Token、Token 校验、主播昵称和头像请求现在支持代理设置。

### 质量保障

- 新增错峰间隔为 `0` 的配置测试。
- 通过 Go 全量测试、`go vet`、前端生产构建和 Wails 打包验证。

## 2026-06-07

### 受限直播识别

- 对选择“强制 Cookie”的主播增加受限直播识别。
- 同一场直播连续两次出现 `401`、`403` 或 `404` 后，显示“受限直播：当前账号无观看权限”。
- 进入受限状态后，本场直播不再重复启动 FFmpeg 或发送重复错误提醒。
- 直播结束、开启新直播或鉴权配置变化后，自动恢复检测。

### Cookie HLS 录制

- 修复部分需要 Cookie 的直播能够解析地址，但 FFmpeg 打开 HLS 时返回 `401 Unauthorized` 的问题。
- FFmpeg 不再同时传递显式 Cookie 请求头和原生 Cookie jar，避免部分 HLS 服务拒绝请求。

### 发布与界面

- TG 推送改为按主播启用，未选择的主播默认不推送。
- 开录推送文案改为“开始直播，并已开始录制”。
- 录制历史支持区分手动停止和异常录制。
- 监控页精简录制时长展示，并改善媒体时长出现逻辑。

## 2026-06-06

### 后台与单文件发布

- 调整关闭窗口行为：关闭按钮隐藏到系统托盘，录制和监听继续运行。
- 增加单实例恢复窗口和托盘退出处理。
- worker 支持由主程序自身启动，为单 EXE 发布提供基础。
- 完善 FFmpeg/FFprobe 路径检测和健康检查。
